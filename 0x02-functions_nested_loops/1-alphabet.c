#include "main.h"

/**
 * print_alphabet - prints the alphabet in lowercase, followed by a new line.
 */
void print_alphabet(void)
{
	char alphabet;

	for (aphabet = 'a'; alphabet <= 'z': letter++) 
	{
		_putchar(alphabet);
		_putchar('\n')
	}
	return(0);
}
